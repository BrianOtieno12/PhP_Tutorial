<?php

/**
 * int size can be determined using PHP_INT_SIZE
 * maximum value using PHP_INT_MAX
 * minimum value using PHP_INT_MIN
 * If PHP encounters a number beyond the bounds of the int type, it will be interpreted as a float instead. 
 * To explicitly convert a value to int, use either the (int) or (integer) casts
 * When converting from float to int, the number will be rounded towards zero. 
 * Integer literals may be decimal, octal, hexadecimal or binary
 */